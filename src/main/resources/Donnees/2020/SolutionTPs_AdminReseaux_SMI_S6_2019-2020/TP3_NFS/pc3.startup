ifconfig eth0 192.168.1.3 up


#pour permettre à la machine d'utiliser NFS
/etc/init.d/nfs-common start

#pour monter les répertoires qui se trouvent dans /etc/fstab
#cette commande n'est pas nécessaire lorsqu'on travaille dans une machine réelle
mount -a
